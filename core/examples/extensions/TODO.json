{
    "title": "TODO",
    "description": "TODO.",
    "type": "object",
    "required": ["type", "coordinates"],
    "properties": {
        "type": { "type": "string", "enum": ["TODO"] },
        "coordinates": {
            "type": "array",
            "minItems": 3,
            "maxItems": 3,
            "items": {"$ref": "#/$defs/position"}
        },
        "bbox": {"$ref": "#/$defs/bbox"}
    }
}